name: Update WakaTime Stats

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - uses: anmol098/waka-readme-stats@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GH_PAT }}

          TIME_RANGE: today

          # 👇 تحسينات من النسخة القديمة
          SHOW_TITLE: "True"
          SECTION_NAME: "⏱️ Weekly Coding Stats"
          LANG_COUNT: "7"
          SHOW_TOTAL_CODE_TIME: "True"
          IGNORED_REPOS: "test,setup"
          IGNORED_LANGUAGES: "JSON,Markdown,YAML,TOML"
          
          # 👇 إعدادات ضرورية تعمل بدون مشاكل
          SHOW_TIMEZONE: "True"
          SHOW_PROJECTS: "True"
          SHOW_LANGUAGE: "True"
          SHOW_EDITORS: "True"
          SHOW_PROFILE_VIEWS: "False"
          SHOW_UPDATED_DATE: "False"
          SHOW_COMMIT: "False"
          SHOW_LANGUAGE_PER_REPO: "False"
          SHOW_LINES_OF_CODE: "False"
          SHOW_LOC_CHART: "False"
          SHOW_DAYS_OF_WEEK: "False"
